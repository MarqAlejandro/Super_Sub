package com.plurasight.ingredient;

public class Meat extends Topping{

    //I created this class as a shell in case additional details needed to be added to just the meats

    public Meat(String type, double price) {
        super(type, price);
    }
}
